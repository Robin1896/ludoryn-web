name: Keep HuggingFace Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -fsS --max-time 30 https://robin1896-ludoryn-web.hf.space/api/ping && echo "OK" || echo "timeout (waking up)"
