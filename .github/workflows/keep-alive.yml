name: Keep Render Alive

on:
  schedule:
    - cron: '*/5 * * * *'
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping
        run: curl -fsS --max-time 30 https://ludus-gh4z.onrender.com/api/ping && echo "OK" || echo "timeout (waking up)"
